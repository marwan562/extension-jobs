# Wuzzuf connection

Wuzzuf is a local browser connection, not a native OAuth integration. Open an existing user-controlled Chrome profile, navigate to Wuzzuf login, complete login or any security verification manually, then verify authentication. Only a local connection reference and normalized state are persisted; passwords, cookies, and challenge tokens are not stored.
